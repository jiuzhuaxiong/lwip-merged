language: c
os: linux
sudo: false

compiler:
 - clang
 - gcc

addons:
  apt:
    sources:
     - llvm-toolchain-precise-3.7
    packages:
     - check
     - libsubunit-dev
     - clang-3.7

script: ./test.sh

